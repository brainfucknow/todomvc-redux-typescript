import { todoApiSteps } from './todo-api'

/**
 * The one step vocabulary the generated entry points run against. A second
 * family of features gets a second module here and a suite that merges the
 * definition lists.
 */
export const projectSteps = todoApiSteps
